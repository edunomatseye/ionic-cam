import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.teotechng.org',
  appName: 'p-gallery',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
